language: bash
before_install:
  - sudo Bash-Snippets/./install.sh all
  - sudo add-apt-repository ppa:duggan/bats --yes
  - sudo apt-get update -qq
  - sudo apt-get install -qq bats
script:
- bats tests
